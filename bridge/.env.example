# Insomnia Bridge Environment Variables
# Copy this file to .env and fill in your values

# Telegram Bot Token (from @BotFather)
# REQUIRED: Get this by creating a bot with @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Bridge Directory (optional)
# The bridge will auto-detect this, but you can override if needed
# BRIDGE_DIR=/path/to/insomnia/bridge

# Orchestrator Directory (optional)
# The orchestrator will auto-detect this, but you can override if needed
# ORCHESTRATOR_DIR=/path/to/insomnia/orchestrator

# Automation System Root (optional)
# Root directory for the entire insomnia system
# AUTOMATION_SYSTEM_DIR=/path/to/insomnia
