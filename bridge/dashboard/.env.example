# Dashboard API Authentication
# Set this to a secure random string to enable API key authentication
# Without this, only localhost requests are allowed for write operations
#
# Generate a secure key: openssl rand -hex 32
#
# DASHBOARD_API_KEY=your-secure-api-key-here

# Usage:
# curl -X POST http://localhost:3333/api/bridge/restart \
#   -H "Authorization: Bearer your-secure-api-key-here"
